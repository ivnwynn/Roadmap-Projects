# Guide for Using the Archiving Tool

**Note:** Before you run the `.sh` file, you need to add an argument after it.

Example:

```bash
bash Log_Archive.sh "your directory here that you want to archive as a log"

- **`[Project Documentation]`** → this is the text that will be clickable  
- **`(https://github.com/ivnwynn/Roadmap-Projects/blob/main/Log_Archive_Project/Log_Archive_Tool.sh)`** → the link it points to  
