# Guide for Using the Archiving Tool

**Note:** Before you run the `.sh` file, you need to add an argument after it.

Example:

```bash
bash Log_Archive.sh "your directory here that you want to archive as a log"
